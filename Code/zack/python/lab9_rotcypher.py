"""
Write a program that prompts the user for a string, and encodes it with ROT13. 
For each character, find the corresponding character, add it to an output string. 
Notice that there are 26 letters in the English language, so encryption is the same 
as decryption.
"""
import string

# letters
english = list(string.ascii_lowercase)

# functions
# def cypher(letter, pos):
#     new_letter = ''
#     # find the letter
#     for i in range(len(english)):
#         if letter in english:
            
            
    

print(english)
# # message
# prompt = input('Message to encrypt: ')
# prompt = list(prompt)

# # cyphering message
# for i in range(len(prompt)):
#     prompt[i] = cypher(prompt[i], 13)
# prompt = str(prompt)

# test
# print(prompt)









# # functions
# # position change
# def change_over(i):
#     count = 0
#     while count < 13:
#         ...
# # i += 13
# # LOOP if count > range(len(english)):
#     # count = 0

# # message function
# def cypher(message):
#     message = list(message)
#     for i in range(len(message)):
#         change_over(i)
#     return str(message)






# # user input
# prompt = input('Message to encode: ').lower


# print(english)


